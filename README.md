# UniAdmit 🎓

UniAdmit - это полнофункциональное веб-приложение для управления процессом поступления абитуриентов в университет. Система предоставляет REST API для работы с данными абитуриентов, факультетов, учебных групп, расписанием экзаменов и оценками. Включает генерацию тестовых данных, демонстрационные запросы и бенчмаркинг производительности.  

### Вариант 1: Локальная установка с виртуальным окружением и бд

#### Предварительные требования:
- Python 3.12
- PostgreSQL
- pip (менеджер пакетов Python)

#### Шаги установки:

1. **Клонирование и настройка окружения:**
```bash
git clone https://github.com/Brynik1/UniAdmit.git
cd UniAdmit
python -m venv venv
source venv/bin/activate  # Linux/MacOS
# или
venv\Scripts\activate     # Windows
pip install -r requirements.txt
```
2. **Создание базы данных**
```bash
psql -U postgres -c "CREATE DATABASE university_db;"
```
3. **Заполнение файла .env (по примеру .env.example**
4. **Запуск приложения**
```bash
# Заполнение БД тестовыми данными и запуск API
python main.py --sample --api

# Только демонстрационные запросы
python main.py --demo

# Только API без пересоздания данных
python main.py --api
```

### Вариант 2: Запуск через Docker Compose

#### Предварительные требования:
- Docker
- Docker Compose

#### Шаги запуска:

1. **Клонирование репозитория:**  
```bash
git clone https://github.com/Brynik1/UniAdmit.git
cd UniAdmit
```  
2. **Запуск контейнера:**  
```bash
docker-compose up --build
```  
3. **Проверка работы:**  
Приложение будет доступно по адресу: http://localhost:8000


#### 🛠 Используемый стек технологий  

- Python 3.12 - основной язык программирования
- FastAPI - современный ASGI фреймворк для построения API
- SQLAlchemy - ORM для работы с базой данных
- PostgreSQL - реляционная база данных
- Uvicorn - ASGI сервер для запуска приложения FastAPI

